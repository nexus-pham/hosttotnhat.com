---
import { Icon } from 'astro-icon/components';
interface Props {
  name: string;
  class?: string;
}
const { name, class: className } = Astro.props;
---
<Icon name={`lucide:${name}`} class={className} aria-hidden="true" />
